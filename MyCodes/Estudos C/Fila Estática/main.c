#include <stdio.h>
#include "FilaEstatica.h"

int main(){

Fila *fi;

cria_fila();

int x = tamanho_fila(fi);

if(fila_cheia(fi));
  printf("Fila cheia\n");

if(fila_vazia(fi));
  printf("Fila vazia");




libera_fila(fi);

  return 0;
}